# include<stdio.h>
// In for loop all int part,condition(a<10),and step(++i) all comes in only single bracket for(int part,condition,step).
int main(){
    for(int a = 0; a<10; ++a)
    {
        printf("The value of a is %d \n",a);
    }
    return 0;
}