# include<stdio.h>

int main(){
    int i = 5;
    // if you want to see result plz uncommet each print block & run.



    // i++ = Pehele print fir increment.
    // printf("The value of  ++i is %d \n",++i);
    // ++i =  pehele increment fir print kare,
    // printf("The value of  i++ is %d \n",i++);

    // --i starts from here.
    
    // i++ = Pehele print fir increment.
    printf("The value of  --i is %d \n",--i);
    // ++i =  pehele increment fir print kare,
    // printf("The value of  i-- is %d \n",i--);

    return 0;
}