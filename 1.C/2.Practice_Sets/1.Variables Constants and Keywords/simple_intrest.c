#include <stdio.h>
int main(){
    int principal=100, rate=4, years=1;
     int sampleintrest = (principal * rate * years / 100);
     printf("The value of simpleintrest is %d \n", sampleintrest);
     return 0;
}